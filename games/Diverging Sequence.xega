#TODO: need to update presentation function heavily to reflect these changes
assign(s=story())
#TODO: need to decide + finalize the baseline comparison here
assign(t1="After the rain", t2=" I eat food")

elicit(x1, 5)
ensure(xed(x1 | s) >= xed(t2 | t1))

elicit(x2, 5)
ensure(xed(x2 | x1 + s) >= xed(t2 | t1))

reward(dex(x2 | s))
